import React from 'react'

const RedirectPage = (): React.ReactElement => {
  return (
        <div>
            <h3 style={{ margin: '1rem' }}>redirect...</h3>
        </div>)
}

export default RedirectPage
